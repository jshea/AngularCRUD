import { browser } from 'protractor';

describe('person', function () {

  beforeEach(function () {
    browser.get('/');
  });

  it('should...', function () {
    expect(true).toEqual(true);
  });
});
